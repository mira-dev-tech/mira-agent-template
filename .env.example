# Copie para .env (NÃO commitar). O .env está no .gitignore.

# Login (Fase atual). Em breve será substituído por login no navegador (mira login abre o browser).
MIRA_EMAIL=
MIRA_PASSWORD=

# Credenciais de integrações MCP (ex.: VTEX). Só usadas por `mira apply`, nunca commitadas.
VTEX_ACCOUNT=
VTEX_APPKEY=
VTEX_APPTOKEN=
